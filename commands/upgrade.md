Upgrade a previously bootstrapped or adopted project to reflect evolved architecture rules.

Diffs the current architecture docs against the project's existing review checklist, identifies new and changed rules, and proposes incremental changes using the adopt-style workflow.

Use the `upgrade` skill to perform this task.
