Adopt the opinionated TypeScript architecture in an existing project.

Run a review first, then propose incremental changes. Tracks accepted and discarded proposals in `tseng/adoption.md` so progress is preserved across sessions.

Use the `adopt` skill to perform this task.
