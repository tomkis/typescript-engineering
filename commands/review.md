Review an existing TypeScript project against the opinionated architecture rules.

Audit the project's monorepo structure, three-layer architecture, stack choices, and dependency rules. Produce a structured report of passes, violations, and suggestions.

Use the `review` skill to perform this task.
