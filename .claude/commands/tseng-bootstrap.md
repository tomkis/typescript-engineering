Bootstrap a new greenfield TypeScript client-server project.

Scaffold an opinionated monorepo with server and client packages, tRPC, Zod, and a DDD-inspired layered architecture. Persist the project layout into `tseng/project-structure.md`.

Use the `tseng-bootstrap` skill to perform this task.
