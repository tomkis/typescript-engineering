Bootstrap or validate the current project against the TSEng opinionated monorepo architecture stack.

If the project is empty or has no structure yet, scaffold it as a monorepo with server and client packages, tRPC, Zod, and the DDD-inspired layered architecture.

If the project already has structure, validate it against the architecture requirements and report compliance.

In both cases, persist the discovered (or created) project layout into `tseng/project-structure.md`.

Use the `tseng-bootstrap` skill to perform this task.
